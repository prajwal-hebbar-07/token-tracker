{
  "name": "token-tracker",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "turbo build",
    "dev": "pnpm --filter @token-tracker/desktop run dev",
    "bundle": "pnpm --filter @token-tracker/desktop run bundle",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "2.10.10"
  }
}
